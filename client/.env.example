# Where the Colyseus server lives. Baked in at build time by Vite.
# Local dev leaves this unset and defaults to ws://localhost:2567.
# Production must use wss:// (a https:// page cannot open a ws:// socket).
VITE_SERVER_URL=wss://cluckdown-server.onrender.com
